# API Configuration (Local Development)
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000

# Public Site URL (used for canonical, robots Host/Sitemap, sitemap URLs)
# Set this to your production domain in hosting env (e.g., Vercel), or locally in .env.local
# Example:
# NEXT_PUBLIC_SITE_URL=https://your-domain.com

# Optional X/Twitter handle for attribution on link previews
# Example:
# NEXT_PUBLIC_TWITTER_HANDLE=starcyeed

# Production URLs (Custom Domain)
# NEXT_PUBLIC_API_URL=https://api.starcyeed.com
# NEXT_PUBLIC_WS_URL=wss://api.starcyeed.com

# Note: Production values are set in vercel.json for Vercel deployment
# For local testing against production backend, uncomment the custom domain URLs above